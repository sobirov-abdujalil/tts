# Copy to .env for local development. Real secrets NEVER go in this file or git.
# Everything below is unused until the milestone noted; keep placeholders honest.

# apps/api — HTTP port (M1)
# PORT=3001

# Infrastructure via docker-compose.yml (used from M5)
# DATABASE_URL=postgres://tts:tts@localhost:5432/tts
# REDIS_URL=redis://localhost:6379
